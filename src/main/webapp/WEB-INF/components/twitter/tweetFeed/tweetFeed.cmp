<aura:component>
	<aura:handler event="twitter:tweetMessageEvent" action="{!c.receiveMessage}" />
    <aura:attribute name="number" type="String" default="No Phone Number"/>
    <ui:outputText aura:id="message" value="{!v.number}"/>
    <h2>Feed</h2>
    {!v.body}
</aura:component>